TebLocalPlannerROS:

  # 轨迹配置
  teb_autosize: true
  dt_ref: 0.3
  dt_hysteresis: 0.1
  max_samples: 500
  global_plan_overwrite_orientation: true
  allow_init_with_backwards_motion: false
  max_global_plan_lookahead_dist: 3.0
  global_plan_viapoint_sep: -1.0
  global_plan_prune_distance: 1.0
  exact_arc_length: false
  feasibility_check_no_poses: 5
  publish_feedback: false

  # 机器人配置（Jackal）
  max_vel_x: 0.5
  max_vel_x_backwards: 0.2
  max_vel_y: 0.0            # 差速驱动无横向速度
  max_vel_theta: 1.0        # 比默认值略低，减少旋转时NDT漂移
  acc_lim_x: 0.5
  acc_lim_theta: 0.5        # 降低角加速度，让旋转更平滑
  min_turning_radius: 0.0   # 差速驱动可原地转
  wheelbase: 0.0
  cmd_angle_instead_rotvel: false

  # 目标容差
  xy_goal_tolerance: 0.3
  yaw_goal_tolerance: 2.5   # 约143°：忽略到达朝向，位置到了即走
  free_goal_vel: false
  complete_global_plan: true

  # 障碍物配置
  min_obstacle_dist: 0.22        # 略大于机器人半宽（0.21m）
  inflation_dist: 0.4
  dynamic_obstacle_inflation_dist: 0.6
  include_dynamic_obstacles: false
  include_costmap_obstacles: true
  costmap_obstacles_behind_robot_dist: 1.5
  obstacle_poses_affected: 30
  costmap_converter_plugin: ""
  costmap_converter_spin_thread: true
  costmap_converter_rate: 5

  # 优化配置
  no_inner_iterations: 5
  no_outer_iterations: 4
  optimization_activate: true
  optimization_verbose: false
  penalty_epsilon: 0.1
  obstacle_cost_exponent: 4
  weight_max_vel_x: 2.0
  weight_max_vel_theta: 1.0
  weight_acc_lim_x: 1.0
  weight_acc_lim_theta: 1.0
  weight_kinematics_nh: 1000.0  # 非完整约束（差速）
  weight_kinematics_forward_drive: 1.0
  weight_kinematics_turning_radius: 1.0
  weight_optimaltime: 1.0
  weight_shortest_path: 0.0
  weight_obstacle: 50.0
  weight_inflation: 0.1
  weight_dynamic_obstacle: 10.0
  weight_dynamic_obstacle_inflation: 0.2
  weight_viapoint: 1.0
  weight_adapt_factor: 2.0

  # 拓扑规划：关闭多路径，节省CPU（单路径对走廊环境已够用）
  enable_homotopy_class_planning: false
  enable_multithreading: false
  max_number_classes: 1
  selection_cost_hysteresis: 1.0
  selection_prefer_initial_plan: 0.95
  selection_obst_cost_scale: 1.0
  selection_alternative_time_cost: false
  roadmap_graph_no_samples: 15
  roadmap_graph_area_width: 5.0
  roadmap_graph_area_length_scale: 1.0
  h_signature_prescaler: 0.5
  h_signature_threshold: 0.1
  obstacle_heading_threshold: 0.45
  switching_blocking_period: 0.0
  viapoints_all_candidates: true
  delete_detours_backwards: true
  max_ratio_detours_duration_best_duration: 3.0
  visualize_homotopy_classes: false
  visualize_with_time_as_z_axis_scale: 0.0

  # 恢复行为
  shrink_horizon_backup: true
  shrink_horizon_min_duration: 10.0
  oscillation_recovery: true
  oscillation_v_eps: 0.1
  oscillation_omega_eps: 0.1
  oscillation_recovery_min_duration: 10.0
  oscillation_filter_duration: 10.0
